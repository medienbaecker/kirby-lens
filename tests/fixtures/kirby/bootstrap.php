<?php

/**
 * Never loaded. The extension treats a workspace as a Kirby project when it can
 * find a Kirby to bootstrap, the same test the generator makes, so the fixture
 * needs one to be recognised at all.
 */
